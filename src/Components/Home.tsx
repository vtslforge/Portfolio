import Navbar from "./Navbar"

const Home = () => {
  return (
    <div className='[background:var(--color-mainBackground)] h-screen w-screen justify-center flex'>
      <Navbar/>
    </div>
  )
}

export default Home
